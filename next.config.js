/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['zonatello.com'], // For case study images if needed
  },
}

module.exports = nextConfig
